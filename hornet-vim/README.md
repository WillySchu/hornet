# Hornet Vim support

Files:
- `syntax/hornet.vim` — syntax highlighting for Hornet (`*.ht`)
- `ftdetect/hornet.vim` — detects `*.ht` as `filetype=hornet`
- `indent/hornet.vim` — indentation support for Hornet's colon/indent blocks

Install into a Vim runtime directory, e.g.:
`~/.vim/`
