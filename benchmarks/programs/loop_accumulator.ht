def int main():
    int sum = 0
    int i = 0
    while i < 50000000:
        int scratch
        int before = scratch
        scratch = i % 7
        sum = (sum + before + scratch) % 1000003
        i = i + 1
    return sum % 256
